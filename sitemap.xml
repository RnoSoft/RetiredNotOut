---
layout: null
no_index: true;
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {%- assign _pages = site.pages | concat: site.posts | sort: "url" -%}
    {%- for page in _pages -%}
        {%- include set-no-index.html -%}
        {%- if _no_index -%}
            {%- continue -%}
        {%- endif -%}
        {%- if page.no_index -%}
            {%- continue -%}
        {%- endif -%}
        {%- if page.url == "/index" -%}
            {%- assign _url = "" -%}
        {%- else -%}
            {%- assign _url = page.url -%}
        {%- endif %}
        <url><loc>https://retirednotout.uk{{_url}}</loc></url>
    {%- endfor %}
</urlset>
